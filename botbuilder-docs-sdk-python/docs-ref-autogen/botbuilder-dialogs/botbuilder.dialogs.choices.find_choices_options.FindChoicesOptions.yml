### YamlMime:PythonClass
uid: botbuilder.dialogs.choices.find_choices_options.FindChoicesOptions
name: FindChoicesOptions
fullName: botbuilder.dialogs.choices.find_choices_options.FindChoicesOptions
module: botbuilder.dialogs.choices.find_choices_options
summary: Contains options to control how input is matched against a list of choices
constructor:
  syntax: 'FindChoicesOptions(no_value: bool = None, no_action: bool = None, recognize_numbers:
    bool = True, recognize_ordinals: bool = True, **kwargs)'
  parameters:
  - name: no_value
    defaultValue: None
    types:
    - '(<xref:Optional>)<xref: If `True`, <xref:botbuilder.dialogs.choices.find_choices_options.the
      choices `value> field will NOT be search over. Defaults` to False.'
  - name: no_action
    description: Defaults to *False*.
    defaultValue: None
    types:
    - '(<xref:Optional>)<xref: If `True`, <xref:the choices `action.title> field will
      NOT be searched over.`'
  - name: recognize_numbers
    defaultValue: 'True'
    types:
    - '(<xref:Optional>)<xref: Indicates whether the recognizer should check for Numbers
      using the>'
  - name: NumberModel.
    isRequired: true
    types:
    - <xref:<xref:NumberRecognizer's>>
  - name: recognize_ordinals
    defaultValue: 'True'
    types:
    - '(<xref:Options>)<xref: Indicates whether the recognizer should check for Ordinal
      Numbers using>'
  - name: OrdinalModel.
    isRequired: true
    types:
    - <xref:<xref:the NumberRecognizer's>>
